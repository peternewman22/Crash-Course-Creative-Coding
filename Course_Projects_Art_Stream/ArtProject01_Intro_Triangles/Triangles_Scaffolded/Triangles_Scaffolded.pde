color yellow = #FAC22B;
color purple = #603E95;
color red = #D7255B;
color green = #009DA1;

void setup(){
  size(400,400);
  //stroke(   );
  //strokeWeight(   );
  strokeJoin(BEVEL); // this is to stop a corner sticking into the yellow section
}

//uncomment using CTRL+/ and write in the coordinates. Hint: copy-paste is better than retyping

void draw(){
  background(255);
  fill(yellow);
  //triangle(    );
  fill(purple);
  //triangle(    );
  fill(green);
  //triangle(    );
  fill(red);
  //triangle(    );
  

}
